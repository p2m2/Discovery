package inrae.semantic_web.sparql

import utest._

object QueryRunnerTest extends TestSuite {

  def tests = Tests {
    test("Create a Query Runner") {
      //val queryRunner = new QueryRunner()
      //val r = queryRunner("h1")
    }

    test("Create a query finding a subject") {

    }
  }
}